Create new step function state machine, a workflow that will run ocassionally and perform fundamental analysis on Issuers. for now it will run on demand only.
it should take parameters: name of issuers to analyse, or alternatively number of issuers to analyse - default 2, and the issuers will be selected rendomly from specific issuers in order of when they were analysed last time. Basically only corporate companien should be taken into account.
Selecting issuers would the the first step
Second is the analysis of selected issuers.
Analysis should use the same logic as 'scripts\analyze-issuer\analyze-issuer.ts' and result in analysis report being stored in IssuerProfiles table.

Update main readme file with information about parameters for new state machine.