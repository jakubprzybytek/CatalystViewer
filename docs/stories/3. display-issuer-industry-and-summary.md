# Story: Display Issuer Industry and Summary

## Background

Story 2 introduced the `IssuerProfiles` DynamoDB table, which stores an AI-generated `industry` label and `businessSummary` for each issuer. Currently this data is never surfaced in the frontend. The `IssuerCard` component shows the issuer name and financial metrics but has no context about what the company actually does.

---

## Goal

Enrich `IssuerCard` with two pieces of profile information:

- **Industry label** — always visible, displayed as a small chip/badge below the issuer name.
- **Business summary** — shown on demand: the user clicks an info button on the card to open a MUI `Dialog` with the full 2–3 sentence summary.

If no profile exists for an issuer yet (classification still pending), the card renders silently without a chip or button — no placeholder text, no spinner per card.

---

## Data Flow

```
IssuerProfiles DynamoDB table
    ↓ (new Lambda – getIssuerProfiles)
    ↓ (new API Gateway route  GET /api/issuers/profiles)
    ↓ (new SDK function – getIssuerProfiles)
IssuersViewer
    → merge by issuerName into IssuerReport[]
        ↓
IssuerCard  (read industry + businessSummary from IssuerReport)
```

Profile data is fetched once per page load alongside `getBondReports()` — a single parallel `Promise.all` in `BondReportsBrowser`. There is no per-card lazy loading.

---

## Data Model Changes

### `IssuerReport` type

Extend the existing type in `packages/web/src/components/BondReportsBrowser/issuers/index.tsx`:

```ts
export type IssuerReport = {
    // existing fields unchanged
    name: string;
    interestBaseType: string;
    interestConstAverage: number;
    currency: string;
    count: number;
    minNominalValue: number;
    maxNominalValue: number;
    avgIssueValue: number;
    totalIssueValue: number;
    // new optional fields
    industry?: string;
    businessSummary?: string;
};
```

### New SDK type

Add to `packages/web/src/sdk/index.ts` (or a new `Issuers.ts` module):

```ts
export type IssuerProfile = {
    issuerName: string;
    industry: string;
    businessSummary: string;
};
```

---

## Frontend Changes

### 1 — New SDK function `getIssuerProfiles()`

Create `packages/web/src/sdk/Issuers.ts`:
- `GET /api/issuers/profiles` with the same auth header pattern as `getBondReports()`.
- Returns `IssuerProfile[]`.
- On error, logs and returns `[]` so a profiles fetch failure never breaks the bonds view.

### 2 — `BondReportsBrowser` — parallel fetch

In `packages/web/src/components/BondReportsBrowser/index.tsx`:
- Add `const [issuerProfiles, setIssuerProfiles] = useState<IssuerProfile[]>([])`.
- Change the existing `getBondReports()` call to `Promise.all([getBondReports(bondType), getIssuerProfiles()])`.
- Pass `issuerProfiles` down to `IssuersViewer`.

### 3 — `IssuersViewer` — merge profiles

In `packages/web/src/components/BondReportsBrowser/issuers/IssuersViewer.tsx`:
- Accept a new prop `issuerProfiles: IssuerProfile[]`.
- Inside the `useMemo`, build a `Map<string, IssuerProfile>` keyed by `issuerName`.
- When constructing each `IssuerReport`, spread in `industry` and `businessSummary` from the map if a matching profile exists.

### 4 — `IssuerCard` — display industry badge

In `packages/web/src/components/BondReportsBrowser/issuers/IssuerCard.tsx`:

- If `issuerReport.industry` is defined, render a MUI `Chip` (size `small`) below the issuer name `Typography`, aligned left.
- The chip is color-coded by industry using the `sx` prop (background color + contrasting text color). See the color mapping table below.
- If `issuerReport.businessSummary` is defined, render a small MUI `IconButton` (info icon) to the left of the existing `Checkbox`. Clicking it opens a `Dialog`.

#### Industry color mapping

| Industry | Background color | Text color |
|---|---|---|
| `Developer` | `#d32f2f` (red) | `#ffffff` |
| `Finance` | `#1565c0` (blue) | `#ffffff` |
| `Energy` | `#e65100` (deep orange) | `#ffffff` |
| `Health Services` | `#2e7d32` (green) | `#ffffff` |
| `Retail` | `#6a1b9a` (purple) | `#ffffff` |
| `Manufacturing` | `#4e342e` (brown) | `#ffffff` |
| `Municipal` | `#00695c` (teal) | `#ffffff` |
| `Other` | `#546e7a` (blue-grey) | `#ffffff` |

These are proposed defaults — adjust colors as needed. The mapping lives in a small helper (e.g. `industryColor(industry: string): string`) co-located with `IssuerCard` so it can be shared with `IssuerSummaryDialog`.

### 5 — `IssuerSummaryDialog` — new component

Create `packages/web/src/components/BondReportsBrowser/issuers/IssuerSummaryDialog.tsx`:
- Props: `issuerName: string`, `industry: string`, `businessSummary: string`, `open: boolean`, `onClose: () => void`.
- Uses MUI `Dialog` with `DialogTitle` (issuer name), `DialogContent` (color-coded industry chip using the same `industryColor` helper + paragraph with business summary), `DialogActions` (Close button).

---

## Backend Changes

### New Lambda — `getIssuerProfiles`

Create `packages/functions/src/issuers/getIssuerProfiles.ts`:
- Scans the full `IssuerProfiles` DynamoDB table.
- Returns `{ issuerProfiles: DbIssuerProfile[] }`.

### Infrastructure

Update `infra/api.ts`:
- Add `getIssuerProfilesFunction`: handler `packages/functions/src/issuers/getIssuerProfiles.handler`, timeout `10 seconds`, linked to `issuerProfilesTable`.
- Register a new API Gateway route: `GET /issuers/profiles` → `getIssuerProfilesFunction`.

---

## Acceptance Criteria

1. Each `IssuerCard` for which a profile exists shows the industry label as a chip below the issuer name.
2. Each such card also shows a small info icon button. Clicking it opens a dialog with the issuer name as title, the industry chip, and the business summary paragraph.
3. The dialog closes on the Close button or clicking outside.
4. Cards for issuers with no profile record render identically to today — no chip, no button, no empty space.
5. A failure of `GET /api/issuers/profiles` does not break the bonds view; the page loads normally with profiles simply absent.
6. Industry and summary data is fetched once per page load, not per card.

---

## Out of Scope

- Filtering or grouping bonds by industry (separate story).
- Showing profiles in the bond-level view (bonds list or bond cards).
- Re-classifying or editing profiles from the UI.
